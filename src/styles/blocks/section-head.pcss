.section-head {
  font-size: 36px;
  color: $text-color-head;
  margin-bottom: 36px;
  padding-top: 10px;
  &--goods {
    padding-top: 0;
    display: inline-flex;
    @include phones {
      display: none;
    }
  }
  @include phones {
    font-size: 26px;
  }
}